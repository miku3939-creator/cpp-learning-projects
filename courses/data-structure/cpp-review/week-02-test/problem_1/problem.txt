题1_模板类求和

【题目描述】
定义一个类模板 Pair<T>，包含两个数据成员 a 和 b，并提供一个成员函数 sum() 返回它们的和。
给定多组整数输入，每组输入两个整数，请输出它们的和。

【输入格式】
共 6 行，每行两个整数 a 和 b，表示一次测试。

【输出格式】
对于每组输入，输出一行，表示 a+b。

【提示】
可以定义：
template <typename T>
class Pair {
    T a, b;
public:
    Pair(T x, T y): a(x), b(y) {}
    T sum() { return a + b; }
};
